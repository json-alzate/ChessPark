import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'demoB',
  webDir: '../../dist/apps/demoB/browser'
};

export default config;
