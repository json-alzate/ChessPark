import { GameClockPipe } from './game-clock.pipe';

describe('GameClockPipe', () => {
  it('create an instance', () => {
    const pipe = new GameClockPipe();
    expect(pipe).toBeTruthy();
  });
});
